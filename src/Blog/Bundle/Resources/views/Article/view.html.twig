{# src/Blog/Bundle/Resources/view/Article/view.html.twig #}{% extends "BlogBundle::layout.html.twig" %}{% block title %}  Lecture d'une annonce - {{ parent() }}{% endblock %}{% block blog_body %}  <h2>{{ article.title }}</h2>  <i>Par {{ article.author }}, le {{ article.date|date('d/m/Y') }}</i>  <div class="articleSelec">    {{ article.content }}  </div>  <p>    <a href="{{ path('blog_homepage') }}" class="bouton boutonRetour">            Retour à la liste    </a>   <a href="{{ path('blog_edit', {'id': article.id}) }}" class="bouton boutonEdit">           Modifier l'information    </a>    <a href="{{ path('blog_delete', {'id': article.id}) }}" class="bouton boutonSupp">           Supprimer l'information    </a>  </p>{% endblock %}